name       = "nexboost-api"
main       = "index.js"
compatibility_date = "2024-01-01"

# Your custom domain route (once nexboost.io is on Cloudflare)
# routes = [{ pattern = "api.nexboost.io/*", zone_name = "nexboost.io" }]

# Or use the free workers.dev subdomain (no custom domain needed):
# URL will be: https://nexboost-api.<your-subdomain>.workers.dev
